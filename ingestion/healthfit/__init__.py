"""
ingestion.healthfit — Retired pipeline stub.

This module was an early ingestion path for HealthFit JSON exports. It has been
superseded by the direct Apple Health XML parsing pipeline in
``ingestion/apple_health/``, which handles both heart rate and body composition
data (including RENPHO measurements exported via HealthKit).

This directory is retained for import path compatibility. No active ingestion
code lives here.
"""
